#import Sys = (*) from sys;

#load "W:/sss/data/zeugs.sss";

master :: proc(args: [] string) {
    iter 0..args.größe {
        Sys.ausgabe(args[it]);
    }

    falls wahr Sys.ausgabe("wahrhaftig");

    iter args Sys.ausgabe(it);
}

master({ "hallo✨\n", "was✨\n", "machst✨\n", "du\n" });

