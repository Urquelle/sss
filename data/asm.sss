Vec3 :: struktur {
    x, y, z : u64;
}

Wert :: union {
    a : char;
    b : Vec3;
    x : u32;
    y : s64;
    z : bool;
}

master :: proc() -> u64 {
    a : Wert;

    a.b.x = 30;

    res a.b.x;
}

