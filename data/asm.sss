Vec3 :: struktur {
    x, y, z : u64;
}

Wert :: union {
    a : char;
    b : Vec3;
    x : u32;
    y : s64;
    z : bool;
}

master :: proc() -> u64 {
    a : Wert;
    b : u32 = 15;

    a.b.y = 30;

    res (a.b.y + b) / 5;
}

