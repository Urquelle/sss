main :: proc(a: u32, b: u32, c: u32, d: u32, e: u32) -> u32 {
    f: u32 = 7;

    ## if
    falls e > 1 {
        (5 + a) / 2 - b == c;
    }

    ## else if
    sonst falls a < 1 {
        3 + a;
    }

    ## while
    solange f < 8 {
        f = 8;
    }

    g : s32 = 0;

    ## for
    iter 1..3 {
        g = g + 1;
    }

    res 10;
}

main(5, 3, 1, 2, 4);
