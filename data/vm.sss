main :: proc(a: u32, b: u32, c: u32, d: u32, e: u32) -> u32 {
    f: u32 = 7;

    falls e > 1 {
        (5 + a) / 2 - b == c;
    } sonst falls a < 1 {
        3 + a;
    }

    res 10;
}

main(5, 3, 1, 2, 4);
